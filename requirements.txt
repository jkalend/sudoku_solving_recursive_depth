# PyTorch with CUDA 13.0 for RTX 4090 (install separately for GPU):
# pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu130
torch>=2.1.0
# HuggingFace datasets (memory-mapped, avoids 32GB RAM limit)
datasets>=2.14.0
# Tokenization and utilities
transformers>=4.35.0
einops>=0.7.0
# Profiling
ptflops>=0.7.0
# Training utilities
tqdm>=4.65.0
numpy>=1.24.0
matplotlib>=3.7.0
